<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url Reducer</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-cover bg-center bg-no-repeat" style="background-image: url('https://images.unsplash.com/photo-1488554378835-f7acf46e6c98?q=80&w=2071&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D');">

    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl text-center font-bold mb-8 text-red-500">URL Link Reducer</h1>
        
        <form class="flex flex-col items-center" action="/shorturl" method="post">
            <label for="fullurl" class="font-semibold mb-2 text-red-500">Full Url</label>

            <input type="url" name="fullUrl" id="fullUrl" class="rounded-md border border-gray-300 px-4 py-2 w-80 focus:outline-none focus:border-blue-500 mb-4 bg-gray-100 text-gray-900">

            <button type="submit" class="bg-red-500 text-white px-8 py-2 rounded-md hover:bg-red-600 transition duration-300">Reduce</button>
        </form>

        <div class="w-full mt-8">
            <table class="w-full bg-gray-100 shadow-md rounded-lg overflow-hidden">
                <thead class="bg-red-500 text-white">
                    <tr>
                        <th class="px-4 py-2">Url</th>
                        <th class="px-4 py-2">Short Url</th>
                        <th class="px-4 py-2">Clicks</th>
                    </tr>
                </thead>
                <tbody>
                    <% shortUrls.forEach(shortUrl => { %>
                        <tr class="border-b border-gray-200 text-center">
                            <td class="px-4 py-2"><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>

                            <td class="px-4 py-2"><a href="<%= shortUrl.short%>" class="text-red-500 hover:underline"><%= shortUrl.short%></a></td>

                            <td class="px-4 py-2"><%= shortUrl.clicks%></td>
                        </tr>
                    <% }) %>
                    
                </tbody>
            </table>
        </div>
    </div>

</body>
</html>
